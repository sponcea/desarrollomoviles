import 'package:flutter/material.dart';

class VistaCircleAvatar extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('CircleAvatar')),
      body: Center(child: Text('CircleAvatarme')),
    );
  }
}
