import 'package:flutter/material.dart';

class VistaFadeIn extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('FadeIn')),
      body: Center(child: Text('FadeIn')),
    );
  }
}
