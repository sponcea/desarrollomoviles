import 'package:flutter/material.dart';

class VistaFadeInImage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Ejemplo Fade In Image')),
      body: Center(child: Text('Fade In Image!!!')),
    );
  }
}
